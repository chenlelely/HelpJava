# 2.9复制含有随机指针节点的链表☆☆
链表节点：
```java
class Node{
    public int val;
    public Node next;
    public Node rand;
    public Node(int val){this.val = val;}
}
```
- 法1：将每一个节点复制一份val->vall,vall的next和rand都指向null，然后放入map（val,vall）;
````java

````